      <div id="Comment-<%= scribble.scribble_id %>" class="comment">
        <%= simple_form_for([scribble, scribble.posted_comments.build]) do |f| %>
          <%= f.text_area :body,:rows=>2, :placeholder=>"Do you know the answer, #{current_user.first_name}?",:maxlength=>"300" %><br>
			 Your Charisma bid: <%= f.input :ask_charisma, as: :integer, label:false %>
			 Or REECHout to an Expert: <%= f.input :linked_user, as: :string, label:false, disabled:true %>
          <%= hidden_field_tag :scribble_id_val, scribble.scribble_id %>
          <%= hidden_field_tag :commentor_id_val,current_user.beamer_id %>
			 
          <br clear="all">
          <%= f.submit "Provide Solution", :class=>"btn btn-inverse"%>
        <% end %>
      </div>
